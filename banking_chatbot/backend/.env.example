# ==================== DATABASE ====================
# PostgreSQL connection URL (automatically provided by Replit)
DATABASE_URL=postgresql://user:password@host:port/database

# PostgreSQL connection details (automatically provided by Replit)
PGHOST=your_postgres_host
PGPORT=5432
PGUSER=your_postgres_user
PGPASSWORD=your_postgres_password
PGDATABASE=your_database_name

# ==================== SECURITY ====================
# JWT Secret Key for token signing (auto-generated if not provided)
# IMPORTANT: Set a fixed value in production for security
SECRET_KEY=your-secret-key-minimum-32-characters-long

# JWT Algorithm (default: HS256)
ALGORITHM=HS256

# JWT Token Expiration in minutes (default: 30)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== AI / LLM PROVIDERS ====================
# OpenAI API Key (optional - for GPT models)
OPENAI_API_KEY=sk-your-openai-api-key

# OpenAI Model Configuration
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_TEMPERATURE=0.7

# Anthropic API Key (optional - for Claude models)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Anthropic Model Configuration
ANTHROPIC_MODEL=claude-3-sonnet-20240229

# AI Provider Selection (options: openai, anthropic, mock)
# If not set, auto-detects based on available API keys
AI_PROVIDER=openai

# ==================== VECTOR DATABASE (OPTIONAL) ====================
# Qdrant configuration for knowledge base (optional)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=banking_knowledge
QDRANT_API_KEY=your-qdrant-api-key
QDRANT_VECTOR_SIZE=1536

# ==================== APPLICATION ====================
# Environment (development, staging, production)
ENVIRONMENT=development

# CORS Origins (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ==================== EMAIL NOTIFICATIONS ====================
# SMTP Configuration for sending email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password

# Email sender information
FROM_EMAIL=noreply@joxai.com
FROM_NAME=JoxAI Banking Chatbot

# ==================== SMS NOTIFICATIONS (FUTURE) ====================
# Twilio Configuration (when SMS notifications are implemented)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ==================== NOTES ====================
# - Never commit this file with real values to version control
# - Copy this file to .env and fill in your actual values
# - In production, use environment variables provided by your hosting platform
# - SECRET_KEY must be at least 32 characters long for security
# - SMTP configuration is optional but recommended for notifications
# - WebSocket connections use JWT authentication (no additional config needed)
#
# REQUIRED FOR PRODUCTION:
# - DATABASE_URL (or PG* variables)
# - SECRET_KEY (strong random string)
# - ANTHROPIC_API_KEY or OPENAI_API_KEY
#
# OPTIONAL (RECOMMENDED):
# - SMTP_* variables for email notifications
# - Custom CORS_ORIGINS for your domain
